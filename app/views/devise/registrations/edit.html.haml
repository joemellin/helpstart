.span6.offset3  
  %h2
    Edit Profile
  = simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { class: 'form-horizontal' , :method => :put }) do |f|
    = devise_error_messages!
    = f.input :name
    = f.input :bio, as: :text, input_html: {rows: "4"}
    %br
    = f.input :linked_in ,placeholder: 'http://...'
    = f.input :email
    %br
    = f.input :profile_pic, label: "Upload a Profile Image"
    %br
    = f.input :password, lable: "New Password", autocomplete: "off"
    = f.input :password_confirmation, lable: "New Password Confirmation"
    = f.input :current_password
    .form-actions= f.submit "Update", class: "btn btn-success btn-large"