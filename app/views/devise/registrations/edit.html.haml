.span6.offset3  
  %h2
    Edit Application
  = simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { class: 'form-horizontal' , :method => :put }) do |f|
    = devise_error_messages!
    = f.input :name
    = f.input :bio
    = f.input :phone
    = f.input :linked_in
    = f.input :email
    = f.input :password, lable: "New Password", autocomplete: "off"
    = f.input :password_confirmation, lable: "New Password Confirmation"
    = f.input :current_password
    .form-actions= f.submit "Update", class: "btn btn-warning"
  %h3 Cancel my account
  %p
    Unhappy? #{button_to "Cancel my account", registration_path(resource_name), :data => { :confirm => "Are you sure?" }, :method => :delete}.
  = link_to "Back", :back